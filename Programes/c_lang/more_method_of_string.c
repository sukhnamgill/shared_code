#include <stdio.h>
int main (){
    printf("hello world!");
    char sr[10];
    printf("\nEnter The string");
    gets(sr);
    puts(sr);
    printf("%s",sr);
    printf("the Execution is completed!!");
// the return valuse is must in int main fuction in integers
    return 0;
}